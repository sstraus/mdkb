---
id: handoff-2026-08-07-9e68e416
title: Corruption localized to the memory write path; core extraction started; nine stories mapped to three invariants
entry_type: handoff
source_type: auto_extracted
status: active
tags: []
created_at: 1786118168
updated_at: 1786118168
expires_at: 1787327768
---

---
session_id: "9e68e416-b997-4670-88b1-7500a0835b1b"
branch: "main"
created: "2026-08-07T15:56:07.460Z"
trigger: "manual"
---

# Session Handoff

**Summary:** Corruption localized to the memory write path; core extraction started; nine stories mapped to three invariants

## Done

- Mapped rootpage->table on 5 quarantined files across 3 projects: damage is ALWAYS memory_entries/memory_fts_data/memory_embeddings (+ content), always 2nd-reference-to-page. Never doc FTS, never vec0 shadows, never code.sqlite
- Forensic quarantine reports: quick_check output, damaged table names, db/WAL bytes, detecting pid+version (src/store/heal.rs). Rows stepped by hand, not flatten(), so an Err after zero rows is recorded instead of read as no-damage
- Schema v18: memory_au scoped to AFTER UPDATE OF id,title,content,tags. get_entry bumps access_count on every READ, which was rewriting FTS5 segments — every memory read was the heaviest writer in the store. RED proven by reverting the trigger
- src/store/identity.rs: first open records the canonical spelling; a second spelling of the same inode is refused, a moved store is adopted
- tests/db_contention_multiprocess.rs: multi-process harness (the existing one is threads in one process). Checks for *.corrupt-* files, not just final quick_check, so autoheal cannot mask a failure
- First core extraction slice: Context, run_mutation and store layout moved to src/core.rs (336 lines). mcp/daemon no longer reach into cli::handlers for the store handle. 18 test files updated
- plans/mdkb-architecture.md: nine open stories mapped to three broken invariants (one writer / one projection contract / no silent success) plus two papercuts
- Story 014-fdf0 implemented by subagent (schema v19, frontmatter split, bidirectional sync, git-aware breaker, watcher route, code_enabled watcher fix)

## Pending

- EVERYTHING IS UNCOMMITTED on main — needs authorisation. Story 022 covers it
- 019-3248 finish core extraction: handle_init, handle_update, hybrid_search_fts, sync_memory_files still cross from mcp/daemon into cli::handlers
- 020-9824 daemon must refuse a store newer than its binary, and retire when its executable changes
- 021-0636 the hook fallback can never fire
- 023 QA 014-fdf0 against its eight criteria before closing
- 018-56b2 daemon sole writer, 011/012 reporting rail, 015/016/017 fold into the projection contract

## Gotchas

- NOT REPRODUCED. Four experiments all green: 10x400 protocol-participating processes, bare-rusqlite foreign writer, system sqlite3 writer, system sqlite3 + long-lived connection. Absence of reproduction is not proof
- Boss was right that brute force was the wrong axis: in production nobody bombards mdkb, it is a few parallel operations. The variable is a SEQUENCE, not volume. Test version skew, writes inside the quarantine/rename window, migration racing another writer — two processes and three operations, not 40 minutes of CPU
- Story 017-a378 holds the one confirmed mechanism: a raw sqlite3 INSERT into a live store produced the exact field signature in memory_fts_data
- Version skew is real here: daemon up since 08-05 17:11 while target/release/mdkb was rebuilt 08-07 — different builds writing one file with v18/v19 landing in between
- The probes are #[ignore]d hypothesis probes. A green run proves nothing — mdkb cannot stop a foreign process opening its file
- The soak checks integrity only at the end, so a killed run leaves no verdict. Fix that before rerunning
